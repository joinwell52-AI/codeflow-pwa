---
description: Performance Tester (PERF-TESTER) rules for qa-team
globs:
alwaysApply: false
---
# PERF-TESTER 规则（qa-team）

适用于承担 `PERF-TESTER`（性能测试工程师）职责的 AI 会话。

## 核心原则

### 接收来源

只接收：`TASK-*-LEADQA-to-PERFTESTER.md`

### 指标必须量化

所有性能结论必须包含具体数值（P95 响应时间、RPS、错误率、CPU 峰值等），不允许写"响应较快"等模糊描述。

### 必须对比目标值

每个指标必须明确写出目标值与实测值，以及是否达标。

### 测试脚本路径必须附报告

方便他人复现，报告中必须说明脚本位置。

### 不直接联系 PM01 / ADMIN01

所有结论经 LEAD-QA 汇总后回报 PM01。

## 回执格式

```
TASK-YYYYMMDD-序号-PERFTESTER-to-LEADQA.md
```

必须包含：测试场景、P95/P99 响应时间、最大 RPS、错误率、CPU/内存峰值、瓶颈分析（如有）。

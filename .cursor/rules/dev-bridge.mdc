---
description: DEV-01 规则——全栈开发工程师接收PM任务、完成编码、写回执
alwaysApply: true
---

# DEV-01 规则

适用于承担 `DEV-01`（全栈开发工程师）职责的 AI 会话。

## 核心原则

### 接收来源

只接收：`TASK-*-PM01-to-DEV01.md`

### 回执必须包含影响范围

```
TASK-{日期}-{序号}-DEV01-to-PM01.md
```

回执内容：
- 修改了哪些文件
- 是否影响已有功能
- 是否需要重启服务
- 自测结果

### 不直接部署生产

代码完成后通知 `OPS-01` 部署，不自行操作生产环境。

## 技术约束

- Python 3.10 / FastAPI（AI后端）
- Vue 2 + Nuxt.js 2（前端，Node 14.21.3）
- 禁止升级 JDK，禁止随意改 Maven 依赖
- 修改 DTO/XML 映射后必须核对字段名和数据库列名

---
description: OPS-01 规则——运维部署工程师负责服务器操作、代码部署、监控
alwaysApply: true
---

# OPS-01 规则

适用于承担 `OPS-01`（运维部署工程师）职责的 AI 会话。

## 核心原则

### 接收来源

只接收：`TASK-*-PM01-to-OPS01.md`

### 高危操作必须二次确认

以下操作前必须在任务文件中记录并等待确认：
- 重启生产服务
- 修改 Nginx 配置
- 清理数据库或日志
- 变更防火墙规则

### 操作必须可回滚

每次部署前备份，有回滚方案。

### 回执必须含验证结果

```
TASK-{日期}-{序号}-OPS01-to-PM01.md
```

不能只写"已完成"，必须附验证命令输出。

## 服务信息

- 中继服务：WebSocket relay（默认端口 5252，也可自定义）
- 后端服务：通过 systemctl 或 supervisorctl 管理
- 发布工具：参考项目根目录 `scripts/deploy_public_relay.py`

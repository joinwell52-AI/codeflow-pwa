---
type: task
task_id: ID006
from: PM-01
to: OPS-01
priority: P0
status: 待执行
created: 2026-03-19 22:00
---

# TASK-20260319-ID006 · PM-01 → OPS-01

## 任务描述

前端发布：PWA（选3）+ PC端泡泡（选4），把 XiaoAiChat.vue 多图截图改动推到线上。

## 背景

ID005 后端已发布完成，但前端未发布。DEV-01 改了 `XiaoAiChat.vue`（多图截图 `_captureCharts()` + chart_images 传输），已同步到：
- `carmms-web-admin/components/XiaoAiChat.vue`
- `frontend-dev/components/XiaoAiChat.vue`

后端接口向后兼容（chart_images 为可选字段），前端不发也不会报错，只是 PDF 里没有饼图。

## 具体步骤

### 1. 发布手机端 PWA

- 运行 `python ops/ops.py`，选择 **选项 3**（手机端 PWA 发布）
- 确认发布成功

### 2. 发布 PC端泡泡 Widget

- 运行 `python ops/ops.py`，选择 **选项 4**（PC端泡泡 Widget 发布）
- 确认发布成功

### 3. 验证

- 访问线上 PWA，问"查应收"，点下载，确认 PDF 里有饼图+柱状图
- 访问 PC 端泡泡，同样验证
- 如果只能验证一端，至少验证 PWA

## 完成标准

- [ ] PWA 前端已发布（ops.py 选 3）
- [ ] PC端泡泡已发布（ops.py 选 4）
- [ ] 线上验证 PDF 导出含图表

## 说明

- 赛格 PC 前端对接暂不处理，等正式环境再说
- 无新增 npm 依赖

## 报告路径

完成后写报告到：`docs/agents/reports/TASK-20260319-ID006-OPS01-to-PM01.md`

# GitHub 仓库页（About）设置说明

适用于 **`joinwell52-AI/codeflow-pwa`**。网页端无法由本仓库脚本自动写入，请在 GitHub 上手动保存一次。

## 操作路径

1. 打开：<https://github.com/joinwell52-AI/codeflow-pwa>
2. 右侧 **About** 区域 → 点击 **⚙（Edit repository details）**  
   或：**Settings** → **General** → 顶部 **Repository name / Description** 区域。

## 建议填写

### Description（仓库简介，单行）

**中英（推荐，便于搜索）：**

```text
CodeFlow / 码流 — Multi-agent collaboration via file-driven TASK protocol; mobile PWA + PC desktop.
```

**纯中文：**

```text
码流（CodeFlow）— 文件驱动多角色协作；手机 PWA + PC 桌面端，TASK 文件名即协议。
```

### Website（网站）

```text
https://joinwell52-ai.github.io/codeflow-pwa/
```

### Topics（主题标签）

在 About 编辑框的 **Topics** 中逐个添加（回车确认），建议：

`codeflow` `pwa` `cursor` `mcp` `websocket` `multi-agent` `collaboration` `task-protocol` `agent-bridge`

（可按需要删减，不必全部。）

## 与旧名 `bridgeflow-pwa` 的关系

仓库已从 **`bridgeflow-pwa`** 重命名为 **`codeflow-pwa`**。若 Description 仍显示旧名，按上文 **Description** 覆盖保存即可；**不会**随 README 自动更新。

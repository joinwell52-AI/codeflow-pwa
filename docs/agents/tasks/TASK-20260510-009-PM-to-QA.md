---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260510-009
sender: PM
recipient: QA
priority: P0
thread_key: codeflow-v0.2-real-sdk-verdict-acceptance-with-admin-key
references:
  - REPORT-20260510-006-QA-to-PM
  - REPORT-20260510-007-DEV-to-PM
  - TASK-20260510-006-PM-to-QA
  - TASK-20260510-008-PM-to-OPS
layer: governance
---

# TASK-20260510-009：v0.2.0-beta + 真 SDK 验收（A-07~10）

## §一 一句话指令

OPS-008 落地 `v0.2.0-beta` tag 后，**立即启动 v0.2.0-beta 全量验收 + A-07~10 真 SDK verdict**（ADMIN 已在 `codeflow-shell/.env` 提供真 `crsr_*` Cursor API key）。本任务覆盖 v0.2.0-alpha 残余 + v0.2.0-beta 增量。

---

## §二 验收范围（按 docs/test-strategy-v0.2-acceleration.md）

### §2.1 v0.2.0-alpha 残余 — 真 SDK 验证（A-07~10）

> ADMIN key 已就绪：`codeflow-shell/.env` 含 `CURSOR_API_KEY=crsr_*` 69 字符（PM 5/10 02:22 验证 git 安全 + 格式合规）。

| # | 检查项 | 期望 |
|---|---|---|
| A-07 | banner 显示 `Cursor SDK : live (CursorSdkAdapter; apiKey from config, listScope="local")` | ✅ |
| A-08 | drop sample-task → REVIEW-*.md `decision` ≠ `needs_human` | 期望 `approved` / `changes_requested` / `blocked` / `rejected` 之一（真 LLM 评审输出）|
| A-09 | sessions/*.json `sdk_agent_id` 是 UUID 形式（`agent-XXXXXXXX-...`）而**非** `sdk-fake-XXXX` | ✅ 真 SDK 路径 |
| A-10 | `.smoke-test-state/transcripts/run-*.md` 含真 LLM 输出（非 fake setImmediate 路径）| ✅ |

### §2.2 v0.2.0-beta 全量

| # | 检查项 | 来源 | 期望 |
|---|---|---|---|
| B-01 | `pack.cmd` 默认无参 → forwards to `npm start` + 打 spike summary banner | DEV REPORT-007 §四 §4 | ✅ |
| B-02 | `pack.cmd bun` / `sea-cjs` / `sea-esm` 子命令各跑对应 spike | DEV REPORT-007 §四 §4 | 失败但有 NOTE 说明（per spike doc）|
| B-03 | 双包 version 升 `0.2.0-beta` | DEV REPORT-007 §四 §5 | ✅ |
| B-04 | runtime 99/99 测试（含 5 个 MT-2 测试 TS-AW-1~5） | DEV REPORT-007 §三 #5 | ✅ |
| B-05 | typecheck 三包（codeflow-shell + runtime + protocol）0 错 | DEV REPORT-007 §三 #6 | ✅ |
| B-06 | drop sample-task 端到端跑 30 次（concurrent）→ 0 EPERM 复现 | MT-2 实证 | 0 |
| B-07 | `.env.example` 顶部含 DO NOT EDIT 强警告 + `crsr_REPLACE_WITH_YOUR_REAL_KEY` 占位符 | PM 5/10 02:30 修 | ✅ |
| B-08 | `git diff codeflow-shell/.env.example` 在跑全量验收期间始终为空（防 ADMIN 误编辑回潮）| 安全监控 | ✅ |

### §2.3 跨版本回归矩阵

按 §7 跨版本回归 — 跑 **v0.1.0-rc.1 → v0.2.0-alpha → v0.2.0-beta** 三档 inbox/ 数据兼容（旧 task-*.md drop 进新版本能正常处理）。

---

## §三 SAFETY HARD GATE — `.env` 隔离

QA 跑验收期间**严禁**：

- ❌ Read 或 cat `codeflow-shell/.env` 文件内容（会泄露 ADMIN 真 key 到 chat / log）
- ❌ 把 `process.env.CURSOR_API_KEY` 输出到 stdout / 测试 log / REPORT 任何位置
- ❌ 把 `.env` 文件 stat / hash / size 之外的信息记录到 REPORT
- ❌ 跑任何 `Get-Content .env` / `cat .env` / 显示文件内容的命令

允许的安全验证：
- ✅ `Test-Path codeflow-shell/.env`（验证存在）
- ✅ `Get-Item codeflow-shell/.env | Select Length`（验证非空）
- ✅ `git status --short codeflow-shell/.env`（验证 gitignore 工作）
- ✅ 跑 `npm start` 看 banner 输出（banner 不会 echo key 全文，只会显示 `apiKey from config`）

---

## §四 surprise S4 (EPERM) 监控（重申）

QA 跑验收期间**严密监控** EPERM 复现：

- 跑 §2.2 B-06 的 30x 并发循环，期望 0 复现
- 任何 1 次复现都立即记录上下文（时间戳 + 进程并发数 + reconciler 调用栈）
- 若 ≥ 3 次复现，**立即** BLOCKER 给 PM（MT-2 patch 失效信号）

---

## §五 回执

```
docs/agents/tasks/REPORT-20260510-009-QA-to-PM.md
```

格式按 QA-006 9 章模板。**明确给出**：

1. 是否推荐 `v0.2.0-beta` 进入 P3 (relay-bridge) 阶段
2. A-07~10 真 SDK 验收结果（含 `decision` 取值统计 — 注意脱敏，不要 echo session_id 或具体 verdict 内容超过摘要级别）
3. EPERM 监控结果（30x 复现率）
4. PM-修 .env.example 防御加固验收（B-07/B-08）

---

## §六 SLA

≤ 3h（按 QA-004 §三 验收策略摘要的 rc.1 时长上限；A-07~10 真 SDK 是新 lane，所以放宽到 alpha + beta 合并验收时长）。完工立即触发 PM 派 P3 TASK 给 DEV。

PM-01
2026-05-10 02:35 (UTC+8)

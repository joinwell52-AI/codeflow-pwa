---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-026
sender: PM
recipient: OPS
priority: P0
thread_key: s5-phase-e-done-checkpoint-commit
references:
  - REPORT-20260509-024-DEV-to-PM
  - REPORT-20260509-023-OPS-to-PM
  - REPORT-20260509-025-QA-to-PM
  - TASK-20260509-024-PM-to-DEV
layer: worker
---

# Sprint S5 Phase E done checkpoint commit + push origin/backup（gitee G3 跳过）

## 背景

DEV 47 分钟内交付 S5 全套主交付：
- **94/94 测试全过**（Phase A 18 + B 22 + C 14 + D 13 + **E 17** + 跨阶段 sanity 10）
- **17 TS-7.x = 13 PM 派单 + 4 bonus**（超目标 4 测试）
- **7 决策 P/Q/R/S/T/U/V 全部锁定** + 1 个附加修复（Phase D `ReviewEngine.whenSettled` race-loop，post-OPS-023 baseline bug）
- **30x 0 flaky**

PM 独立复核 100% 一致：`npx tsc --noEmit` exit 0 + `npm test` = `tests 94 / pass 94 / fail 0 / 6308ms`。

按第 5 句宪法 PM 自约束「commit 节奏 = PM 自决」立即派 OPS 第六轮 commit。

## ⚠️ Selective add（关键）

按 DEV REPORT-024 §四 + §七建议 + PM 接受：**不在本 commit 范围**：

```
不 add：packages/codeflow-runtime/docs/test-strategy-s3.md   (QA-025 工作 1+2+3 产出，1100 行扩充 — 让 QA 走自己的 commit)
不 add：docs/agents/tasks/REPORT-20260509-025-QA-to-PM.md     (QA 自己 commit)
```

理由：
- 保持 S5 sprint 工作产权清晰（DEV vs QA 工作不混淆）
- QA 工作 4（Phase E 回归）完成后 PM 会派 OPS 下一轮单独 commit QA 范围

## 待 commit 文件清单（约 21 项展开后）

### A. `packages/codeflow-runtime/` 改动 — 13 项 M + 1 子目录新增展开 8 项 = 21 项

```
13 M（DEV S5 范围 + Phase D race-loop 兜底）:
   packages/codeflow-runtime/README.md                              (Phase E 完成态 + 决策 P/Q/R/S/T/U + 验收清单 71→94)
   packages/codeflow-runtime/examples/hello-world.ts                (加 plantDemoSkills + 输出 skills loaded / mcpInjector mode)
   packages/codeflow-runtime/package.json                           (0.1.0-alpha.4 → 0.1.0-alpha.5，无新 deps)
   packages/codeflow-runtime/src/Runtime.ts                         (14 子系统装配 — 加 SkillRegistry/Validator/MCPInjector)
   packages/codeflow-runtime/src/index.ts                           (barrel + skill/* + 3 错误类 + KernelValidationFailureEntry)
   packages/codeflow-runtime/src/registry/AgentRegistry.ts          (+ kernelValidator/mcpInjector 可选参数 + register step 4 + 7)
   packages/codeflow-runtime/src/registry/RuntimeBootstrap.ts       (+ kernel 阶段 + summary 🚫 prefix)
   packages/codeflow-runtime/src/registry/__tests__/AgentRegistry.test.ts    (+ TS-7.12 + 7.12b)
   packages/codeflow-runtime/src/registry/__tests__/RuntimeBootstrap.test.ts (+ TS-7.11 + 7.11b)
   packages/codeflow-runtime/src/registry/errors.ts                 (+ KernelDependencyError + MCPInjectorLiveModeNotImplementedError + SkillSchemaError)
   packages/codeflow-runtime/src/registry/index.ts                  (barrel re-export)
   packages/codeflow-runtime/src/review/ReviewEngine.ts             (whenSettled 改 loop-poll + 5s deadline — 决策 V 附加)
   packages/codeflow-runtime/src/types/state.ts                     (+ KernelValidationFailureEntry + ReconciliationReport.kernel_failures)

1 ?? 展开（src/skill/ 全新子目录 = 8 文件）:
   packages/codeflow-runtime/src/skill/SkillRegistry.ts             (269 行 — 主交付 1)
   packages/codeflow-runtime/src/skill/KernelDependencyValidator.ts  (196 行 — 主交付 2)
   packages/codeflow-runtime/src/skill/MCPInjector.ts               (188 行 — 主交付 3)
   packages/codeflow-runtime/src/skill/index.ts                     (30 行 — barrel)
   packages/codeflow-runtime/src/skill/__tests__/SkillRegistry.test.ts          (TS-7.1~7.4 + 3 bonus)
   packages/codeflow-runtime/src/skill/__tests__/KernelDependencyValidator.test.ts (TS-7.5~7.8 + 7.13 + bonus)
   packages/codeflow-runtime/src/skill/__tests__/MCPInjector.test.ts             (TS-7.9~7.10 + bonus)
   packages/codeflow-runtime/src/skill/__tests__/helpers.ts          (119 行)
```

### B. `docs/agents/tasks/` 流程文档 — 9 项（全部 ??）

```
?? docs/agents/tasks/REPORT-20260509-023-OPS-to-PM.md   (OPS 自己 S4 Phase D done 回执)
?? docs/agents/tasks/REPORT-20260509-024-DEV-to-PM.md   (DEV S5 完成回执)
?? docs/agents/tasks/REPORT-20260509-022-PM-to-ADMIN.md (PM S5 完工里程碑通报 + supersede note)
?? docs/agents/tasks/REPORT-20260509-023-PM-to-ADMIN.md (PM FCoP v1.0 align 紧急请示)
?? docs/agents/tasks/REPORT-20260509-024-PM-to-ADMIN.md (PM ADMIN「按推荐」接受回执 ⭐)
?? docs/agents/tasks/TASK-20260509-026-PM-to-OPS.md     (本任务 — 自包含)
?? docs/agents/tasks/TASK-20260509-027-PM-to-QA.md      (QA 工作 4 + Phase E 回归)
?? docs/agents/tasks/TASK-20260509-028-PM-to-DEV.md     (S6 codeflow-shell MVP ⭐)
?? docs/agents/tasks/DRAFT-20260509-001-PM-to-ADMIN-issue-2-reply.md (issue #2 reply 草稿 ⭐)
```

### C. `docs/design/` 设计文档 — 1 项（??）

```
?? docs/design/v0.2-sprint0-roadmap.md                  (v0.2 sprint 0 路线图 — DRAFT-PM-PROPOSED 状态 ⭐)
```

⚠️ 5/9 23:18 PM 追加：ADMIN 5/9 23:14「按推荐」激活 5 议题后，PM 起草 4 个新文档（REPORT-023 + REPORT-024 + TASK-028 + DRAFT-001 + v0.2-roadmap）— 全部归入本 commit scope。

## 加文件原则

```powershell
# 严格 selective add — 排除 QA 范围
git add packages/codeflow-runtime/README.md
git add packages/codeflow-runtime/examples/hello-world.ts
git add packages/codeflow-runtime/package.json
git add packages/codeflow-runtime/package-lock.json
git add packages/codeflow-runtime/src/

# docs/agents/tasks 选择性 add 9 个文件，排除 REPORT-025-QA-to-PM.md
git add docs/agents/tasks/REPORT-20260509-023-OPS-to-PM.md
git add docs/agents/tasks/REPORT-20260509-024-DEV-to-PM.md
git add docs/agents/tasks/REPORT-20260509-022-PM-to-ADMIN.md
git add docs/agents/tasks/REPORT-20260509-023-PM-to-ADMIN.md
git add docs/agents/tasks/REPORT-20260509-024-PM-to-ADMIN.md
git add docs/agents/tasks/TASK-20260509-026-PM-to-OPS.md
git add docs/agents/tasks/TASK-20260509-027-PM-to-QA.md
git add docs/agents/tasks/TASK-20260509-028-PM-to-DEV.md
git add docs/agents/tasks/DRAFT-20260509-001-PM-to-ADMIN-issue-2-reply.md

# docs/design/ — v0.2 sprint 0 路线图（PM 草拟）
git add docs/design/v0.2-sprint0-roadmap.md

# 验证：staged 应**不**包含 docs/test-strategy-s3.md 和 REPORT-025-QA-to-PM.md
git diff --cached --name-only | Select-String "test-strategy-s3"
# 期望：空输出

git diff --cached --name-only | Select-String "REPORT-20260509-025-QA-to-PM"
# 期望：空输出
```

期望 staged 文件**不**包含：
- `_ignore/` / `private/` / `.codeflow/state/`
- `node_modules/` / `examples/inbox/` / `examples/.codeflow-state/`
- `packages/codeflow-protocol/`
- ⚠️ `packages/codeflow-runtime/docs/test-strategy-s3.md`（QA 范围）
- ⚠️ `docs/agents/tasks/REPORT-20260509-025-QA-to-PM.md`（QA 范围）

期望 staged 文件**包含**：
- `docs/design/v0.2-sprint0-roadmap.md`（5/9 23:18 PM 新加）

## 高危操作判定

按 `ops-bridge.mdc` 4 类高危：全部不涉及 → **不属于高危**。

## 执行命令（按顺序）

### Phase 1：dev env + S5 实证回归（≤ 5 min）

```powershell
git status --short
node --version

cd packages/codeflow-protocol
npm install
npm test
cd ../..
# 期望：5 valid + 3 invalid 全过

cd packages/codeflow-runtime
npm install
npx tsc --noEmit
# 期望：exit 0

npm test
# 期望：tests 94 / pass 94 / fail 0
cd ../..
```

如 Phase 1 任一项失败 → 报告 PM；不 commit。

### Phase 2：commit bundle（selective add；≤ 1 min）

```powershell
# Selective add 见 §加文件原则

git diff --cached --name-only

# 验证排除项（关键）
git diff --cached --name-only | Select-String "test-strategy-s3"
git diff --cached --name-only | Select-String "REPORT-20260509-025-QA"

git commit -m "feat(s5-phase-e): SkillRegistry + KernelDependencyValidator + MCPInjector (stub) + AgentRegistry pre-hook + RuntimeBootstrap kernel audit + Runtime 14-subsystem composition + Phase E demo + 17 TS-7.x tests (94/94) + Phase D whenSettled race-loop fix + ADMIN 5/9 23:14 5-issue acceptance + v0.2 sprint 0 roadmap draft + FCoP issue #2 reply draft"

git log -1 --oneline
git show --stat HEAD | Select-Object -First 5
```

### Phase 3：push origin + backup（gitee G3 跳过；≤ 2 min）

```powershell
git fetch --all
git status -sb

git push origin main
git push backup main

Write-Host "gitee push skipped per HANDOFF-001 G3 decision"

$localHead  = git rev-parse HEAD
$originHead = (git ls-remote origin main) -split "`t" | Select-Object -First 1
$backupHead = (git ls-remote backup main) -split "`t" | Select-Object -First 1
$giteeHead  = (git ls-remote gitee  main) -split "`t" | Select-Object -First 1
Write-Host "local : $localHead"
Write-Host "origin: $originHead"
Write-Host "backup: $backupHead"
Write-Host "gitee : $giteeHead"
```

期望：
- local / origin / backup 三者 HEAD 一致
- gitee 仍 `62532a7…`
- ⚠️ Post-commit `git status --short` 应**仍能看到** `docs/test-strategy-s3.md` (M) + `REPORT-20260509-025-QA-to-PM.md` (??)（QA 范围保留）

## 验收（OPS 必出 10 条验证）

| # | 验收项 | 命令 |
|---|---|---|
| 1 | commit 前文件全在 2 个目录之下 | `git status --short` |
| 2 | Node 版本 ≥ 20 | `node --version` |
| 3 | protocol 测试通过 | `cd packages/codeflow-protocol; npm test` 8/8 PASS |
| 4 | runtime typecheck 通过 | `npx tsc --noEmit` exit 0 |
| 5 | runtime **94 测试**通过 | `npm test` 显示 `tests 94 / pass 94 / fail 0` |
| 6 | commit 文件数符合预期（≈ 27）| `git show --stat HEAD` |
| 7 | commit message 正确 | `git log -1 --pretty=%s` 含 `feat(s5-phase-e)` |
| 8 | origin / backup HEAD = local | `git ls-remote` 头部 hash 对得上 |
| 9 | gitee 仍 `62532a7…` | `git ls-remote gitee main` |
| **10** | **selective-add 排除验证** | `git show --stat HEAD --name-only` 不含 `test-strategy-s3.md` 和 `REPORT-20260509-025-QA-to-PM.md`；post-commit `git status --short` 仍含这两项 |

## 不动手清单

- ❌ 不 add `docs/test-strategy-s3.md`（QA 范围）
- ❌ 不 add `REPORT-20260509-025-QA-to-PM.md`（QA 范围）
- ❌ 不创建 tag（v0.1 整体发布在 S6 收尾时统一 tag）
- ❌ 不 push gitee（G3）
- ❌ 不创建 GitHub Release / Gitee Release
- ❌ 不修改 `.git/config`
- ❌ 不 commit `_ignore/` / `private/` / `node_modules/` / `.codeflow/state/` / `examples/inbox/` / `examples/.codeflow-state/`

## 时间预算

≤ 8 分钟。

## 启动条件

OPS 见到本任务即可立即执行。**不需要等 ADMIN 单独拍板**——按第 5 句宪法永久授权。

## 完成后回执要求

按 `ops-bridge` 规则写 `REPORT-20260509-026-OPS-to-PM.md`，必须包含：

1. 10 条验收命令的实际输出
2. 三仓最终 HEAD hash 对比表
3. 本次 commit 的 SHA-1
4. **特别确认**：`git show --stat HEAD --name-only` 完整 file list — 应**不**含 `test-strategy-s3.md` / `REPORT-20260509-025-QA-to-PM.md`
5. **post-commit `git status --short` 输出** — 应仍含上述两项（QA 范围保留）

---

PM-01 派单。OPS-01 立即执行。回执请落 `REPORT-20260509-026-OPS-to-PM.md`。

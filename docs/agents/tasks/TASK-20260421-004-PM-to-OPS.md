---
protocol: fcop
version: 1
sender: PM
recipient: OPS
thread_key: pypi_doc_patch_fcop_20260421
priority: P2
---

# 派发：发布 `fcop 0.2.2` 文档补丁版

## 目标

让 https://pypi.org/project/fcop/ 的 README 显示最新的三路安装说明 + Cursor Install 按钮。

## 执行步骤

1. `codeflow-plugin/pyproject.toml`：`version = "0.2.1"` → `"0.2.2"`
2. `codeflow-plugin/src/codeflow_mcp/__init__.py`：`__version__ = "0.2.1"` → `"0.2.2"`
3. `cd codeflow-plugin && py -3.10 -m build`
4. `py -3.10 -m twine upload dist/fcop-0.2.2*`（用 `%USERPROFILE%\.pypirc` 里已存的 token）
5. 验证：
   - `curl https://pypi.org/pypi/fcop/json` JSON 里 `info.version == "0.2.2"`
   - 浏览器访问 https://pypi.org/project/fcop/ ，能看到 Install 按钮

## 约束

- 不改任何 `.py` 运行时代码
- 不清理 `dist/` 旧文件（留历史）
- 上传后写 `TASK-20260421-004-OPS-to-PM.md`，附 PyPI 链接、curl 验证输出

---
**签名**：PM-01

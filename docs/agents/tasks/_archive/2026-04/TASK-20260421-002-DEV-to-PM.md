---
protocol: fcop
version: 1
sender: DEV
recipient: PM
thread_key: cleanup_codeflow_mcp_20260421
priority: P3
---

# 回执：已下架 `codeflow_mcp.py`（pyautogui 面板 MCP）

## 任务状态

**已完成**，一次会话内处理完毕。

## 实际修改清单

### 删除

| 文件 | 原大小 |
|---|---|
| `codeflow-plugin/scripts/codeflow_mcp.py` | 3854 B |
| `codeflow-plugin/requirements-codeflow-mcp.txt` | 116 B |

### 修改

| 文件 | 变更 |
|---|---|
| `codeflow-plugin/README.md` | 删除"## 可选：轻量 MCP「只打开本机控制面板」"整节（原第 93–121 行），替换为 4 行简短段落，指向 `codeflow-desktop/cursor-extension/` 下的 VSIX 扩展 |
| `docs/cursor-shortcuts-scope.md` | 第 29 行：`MCP: codeflow_mcp.py` → `Cursor 扩展: codeflow-panel-launcher` |
| `docs/cursor-shortcuts-scope.en.md` | 同上（英文对译） |
| `CONTRIBUTING.md` | 第 51 行：`pip install -r requirements-codeflow-mcp.txt` → `pip install -r requirements.txt`（原本就是 bug，指了已删除的文件作为主 MCP 装法） |

额外发现：`CONTRIBUTING.md` 的 MCP 装法指向的是那个**已下架**脚本的依赖文件。
即便不做这次清理，这条也是错的，顺手改正。

## 是否影响已有功能

**不影响。**

- 主 MCP `codeflow-plugin/scripts/mcp_server.py` 未触碰，语法自检通过
  （`py -3.12 -c "import ast; ast.parse(...)"` OK）。
- 主依赖 `codeflow-plugin/requirements.txt` 未变。
- Cursor 当前 `~/.cursor/mcp.json` 本来就没有 `codeflow-panel` 注册项，无需改 MCP 配置。
- `codeflow-desktop/cursor-extension/`（替代方案 VSIX）未触碰。

## 是否需要重启服务

**不需要。** 删除的是未被引用的独立脚本，当前运行中的主 MCP 不受影响。

## 自测结果

1. **全仓索引**：`rg "codeflow_mcp|requirements-codeflow-mcp|open_codeflow_panel|refresh_codeflow_panel"` → 仅剩 `TASK-20260421-002-PM-to-DEV.md` 一处引用（任务单自身作为变更记录）。
2. **scripts 目录**：只剩 `mcp_server.py` + `on_file_edit.py`。
3. **requirements 文件**：只剩 `requirements.txt`。
4. **主 MCP 语法**：`ast.parse(mcp_server.py)` 通过。
5. **README.md / cursor-shortcuts-scope.(md|en.md) / CONTRIBUTING.md**：UTF-8 编码保持，Markdown 结构完整。

## 风险评估

- **可逆性**：所有删除内容都在 Git 历史里，需要恢复随时 `git checkout`。
- **公开依赖**：该文件从未发布到 PyPI 或作为正式产品宣传过，仅在仓库文档中以"可选"
  出现。外部用户若自行配置过，会在 Cursor 启动时看到找不到脚本的报错——属于可接受
  噪音，README 已同步更新引导到 VSIX 方案。
- **符合 `codeflow-project.mdc` 规范**：本次修改的 `.md` / `.py` 文件含中文，
  全程用文件读写工具（Read / StrReplace / Write）处理，未使用 PowerShell `-replace`
  或 `Set-Content`，UTF-8 编码无破坏风险。

## 建议后续

- 本次清理是 `anti_hang_triage_20260421`（AI 卡死分诊）附带收益之一：
  移除了一个"用 pyautogui 模拟键盘且无超时保护"的潜在卡死源。
- 分诊主线（`TASK-20260421-001-PM-to-ADMIN`）可把这次清理作为第一条已落地的
  缓解措施记入最终报告。
- 建议 ADMIN 在代码审核通过后提交，建议 commit message：
  `chore(plugin): remove unused pyautogui panel MCP (codeflow_mcp.py)`

---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-017
sender: PM
recipient: OPS
priority: P0
thread_key: s3-phase-b-done-checkpoint-commit
references:
  - REPORT-20260509-013-DEV-to-PM
  - REPORT-20260509-017-PM-to-ADMIN
  - TASK-20260509-013-PM-to-DEV
  - TASK-20260509-016-PM-to-DEV
layer: worker
---

# Sprint S3 Phase B done checkpoint commit + push origin/backup（gitee 按 G3 跳过）

## ADMIN 5/9 15:37 拍板

= **「按推荐」全过 4 议题** = REPORT-017 §七 4.1 (Phase B 通过) + 4.2 = B-3（立刻 commit Phase B done）+ 4.3 (Phase C 启动) + 4.4 (v0.2 暂不)。

详见 [`REPORT-20260509-017-PM-to-ADMIN.md`](./REPORT-20260509-017-PM-to-ADMIN.md)。

## 待 commit 文件清单（25 项）

按 `git status --short` 当前可见。**OPS 无需 hardcode 文件名**——只需保证 `git add` scope 落在以下 3 类路径，参见 §"加文件原则"：

### A. `packages/codeflow-runtime/` 改动 — 18 项（13 M + 5 ??）

```
M  packages/codeflow-runtime/README.md                                   (Phase B 完成态全量改写)
M  packages/codeflow-runtime/package.json                                (0.1.0-alpha.1 → 0.1.0-alpha.2 + test glob)
M  packages/codeflow-runtime/src/index.ts                                (Phase B 类型 barrel)
M  packages/codeflow-runtime/src/registry/AgentSdkAdapter.ts             (+ send 接口/InMemoryRunHandle/钩子)
M  packages/codeflow-runtime/src/registry/RuntimeBootstrap.ts            (TS-2.8 patch try-catch)
M  packages/codeflow-runtime/src/registry/__tests__/PersistentStore.test.ts  (+ scenario 11 TS-1.6)
M  packages/codeflow-runtime/src/registry/__tests__/RuntimeBootstrap.test.ts (+ scenario 12 TS-2.8)
M  packages/codeflow-runtime/src/registry/__tests__/helpers.ts           (Windows EBUSY retry)
M  packages/codeflow-runtime/src/registry/errors.ts                      (+ 2 个 session 错误类)
M  packages/codeflow-runtime/src/registry/index.ts
M  packages/codeflow-runtime/src/session/SessionManager.ts               (6 方法 method body)
M  packages/codeflow-runtime/src/session/index.ts
M  packages/codeflow-runtime/src/types/state.ts                          (RuntimeEventType + RunHandle.onEvent)

?? packages/codeflow-runtime/src/_internal/                              (atomic-write.ts helper)
?? packages/codeflow-runtime/src/session/SdkRunHandle.ts                 (Cursor SDK Run 包装)
?? packages/codeflow-runtime/src/session/SessionStore.ts                 (单 record per file)
?? packages/codeflow-runtime/src/session/TranscriptWriter.ts             (append-only stream)
?? packages/codeflow-runtime/src/session/__tests__/                      (3 个新测试 + helpers)
```

### B. `docs/design/` 设计文档 — 1 项

```
M  docs/design/codeflow-v2-on-fcop-sdk.md   (DEV 加 §0.0 第 3 句 + §3.0 哲学节；PM 加 §0.0 第 4 句 + 5 行解读表)
```

### C. `docs/agents/tasks/` 流程文档 — 6 项（全部 ??）

```
?? docs/agents/tasks/REPORT-20260509-013-DEV-to-PM.md     (DEV Phase B 完成回执)
?? docs/agents/tasks/REPORT-20260509-015-OPS-to-PM.md     (OPS 自己写的 TASK-015 回执 — 延 commit)
?? docs/agents/tasks/REPORT-20260509-017-PM-to-ADMIN.md   (PM 验收 + 4 议题报送)
?? docs/agents/tasks/TASK-20260509-017-PM-to-OPS.md       (本任务派单 — Phase B done checkpoint commit)
?? docs/agents/tasks/TASK-20260509-018-PM-to-DEV.md       (Phase C 派单 — 待 OPS commit 完后 DEV 开干)
?? docs/agents/tasks/TASK-20260509-019-PM-to-QA.md        (Phase C 测试场景补全派单)
```

OPS 执行前**必须**用 `git status --short | Measure-Object -Line` 验证总数 = **25**（13 M + 11 ?? 折叠目录后）。

## 加文件原则

```powershell
# 三个目录的所有改动一次添加
git add packages/codeflow-runtime/
git add docs/design/
git add docs/agents/tasks/

# 验证 staged 范围
git diff --cached --name-only
```

期望：staged 文件**全部**位于 3 个目录之下，**不**包含：
- `_ignore/` / `private/` / `.codeflow/state/`
- `node_modules/`
- `packages/codeflow-protocol/`（本批不应有改动；如有，停手）

## 高危操作判定

按 [`ops-bridge.mdc`](../../.cursor/rules/ops-bridge.mdc) 4 类高危：全部不涉及 → **不属于高危**。

## 执行命令（按顺序）

### Phase 1：dev env + Phase B 实证回归（≤ 5 min）

```powershell
# 1.1 验证 staged scope
git status --short
# 期望：约 24-25 行（13 M + 11 ?? 折叠目录；§A 5 个 ?? + §C 6 个 ?? = 11）

# 1.2 Node 版本
node --version
# 期望：major >= 20

# 1.3 packages/codeflow-protocol 仍稳
Set-Location packages/codeflow-protocol
npm install
npm test
Set-Location ../..
# 期望：5 valid + 3 invalid 全过，exit 0

# 1.4 packages/codeflow-runtime typecheck + 40 测试
Set-Location packages/codeflow-runtime
npm install
npx tsc --noEmit
# 期望：exit 0，0 行输出

npm test
# 期望：tests 40 / pass 40 / fail 0 / duration_ms 大约 3500-5000
Set-Location ../..
```

如果 Phase 1 任一项失败 → **停下不 commit**，立刻在 `REPORT-017-OPS-to-PM` 描述失败现象。

### Phase 2：commit bundle（≤ 1 min）

```powershell
# 2.1 加文件
git add packages/codeflow-runtime/
git add docs/design/
git add docs/agents/tasks/

# 2.2 验证 staged 范围
git diff --cached --name-only

# 2.3 commit
git commit -m "feat(s3-phase-b): SessionManager + SessionStore + TranscriptWriter + L2 design philosophy + TS-2.8 + TS-1.6 (40/40 tests)"

# 2.4 验证
git log -1 --oneline
git show --stat HEAD | Select-Object -First 5
```

### Phase 3：push origin + backup（gitee G3 跳过；≤ 2 min）

```powershell
git fetch --all
git status -sb
# 期望：含「ahead 1」

git push origin main
git push backup main

Write-Host "gitee push skipped per HANDOFF-001 G3 decision (gitee remains diverged at 62532a7)"

# 三仓 HEAD 对比
$localHead  = git rev-parse HEAD
$originHead = (git ls-remote origin main) -split "`t" | Select-Object -First 1
$backupHead = (git ls-remote backup main) -split "`t" | Select-Object -First 1
$giteeHead  = (git ls-remote gitee  main) -split "`t" | Select-Object -First 1
Write-Host "local : $localHead"
Write-Host "origin: $originHead"
Write-Host "backup: $backupHead"
Write-Host "gitee : $giteeHead"
```

期望：
- local / origin / backup 三者 HEAD 一致（新 commit hash）
- gitee 仍 `62532a7…`

## 失败处理

| 错误 | 处理 |
|---|---|
| Phase 1.4 `npm test` 40 测试出现 fail | 报告 PM；不 commit |
| Phase 1.4 typecheck 失败 | 同上（不应该发生 — DEV REPORT-013 + PM 自跑都已验证） |
| Phase 1.3 protocol 测试失败 | 同上 |
| Phase 2.3 commit 被 pre-commit hook 拦 | 看 hook 输出，**不**用 `--no-verify`；报告 PM |
| Phase 3.2 origin `non-fast-forward` | 停手报告 PM（不擅自 rebase） |
| Phase 3.3 backup 鉴权失败 | 重试 1 次；仍失败 → 报告（origin 成功 = 不阻塞总任务） |
| 网络超时 | 重试 1 次；仍失败 → 报告 |

## 验收（OPS 必出 9 条验证）

| # | 验收项 | 命令 |
|---|---|---|
| 1 | commit 前文件 ≈ 25 项，全在 3 个目录之下 | `git status --short` |
| 2 | Node 版本 ≥ 20 | `node --version` |
| 3 | protocol 测试通过 | `cd packages/codeflow-protocol; npm test` exit 0 + 8/8 PASS |
| 4 | runtime typecheck 通过 | `cd packages/codeflow-runtime; npx tsc --noEmit` exit 0 |
| 5 | runtime 40 测试通过 | `cd packages/codeflow-runtime; npm test` 显示 `tests 40 / pass 40 / fail 0` |
| 6 | commit 文件数 ≈ 25 | `git show --stat HEAD` 显示 `25 files changed` 或同量级（含 _internal/ + __tests__/ 折叠后展开） |
| 7 | commit message 正确 | `git log -1 --pretty=%s` 含 `feat(s3-phase-b)` |
| 8 | origin / backup HEAD = local | `git ls-remote` 头部 hash 对得上 |
| 9 | gitee 仍 `62532a7…` | `git ls-remote gitee main` |

## 不动手清单

- ❌ 不创建 tag（v0.1-alpha.2 等不在本任务范围；S6 完成时统一考虑）
- ❌ 不 push gitee（G3）
- ❌ 不创建 GitHub Release / Gitee Release
- ❌ 不打包 EXE（codeflow-shell 在 S6 才创建）
- ❌ 不动 CHANGELOG.md
- ❌ 不动 main.py VERSION（v1 codeflow-desktop/）
- ❌ 不修改 `.git/config`
- ❌ 不 commit `_ignore/` / `private/` / `node_modules/` / `.codeflow/state/`

## 时间预算

≤ 8 分钟（Phase 1: 5min + Phase 2: 1min + Phase 3: 2min）

## 启动条件

OPS 在以下条件后立刻执行：

1. ✅ **PM 已通知"3 单全部落盘"**（即 PM 完成本 TASK-017 + TASK-018-PM-to-DEV + TASK-019-PM-to-QA 写入后；按 PM 5/9 15:37 派单序列）
2. ✅ **DEV/QA 已知本批 commit 即将发生**——他们的下一波工作（Phase C 实施 + 测试场景补全）会基于这次 commit 之后的工作区基线开始

## 完成后回执要求

按 `ops-bridge` 规则写 `REPORT-20260509-017-OPS-to-PM.md`，**必须**包含：

1. 9 条验收命令的实际输出（粘贴 stdout）
2. 三仓最终 HEAD hash 对比表
3. 本次 commit 的 SHA-1（前 7 位 + 完整 hash）
4. **特别确认**：`git show --stat HEAD` 完整 file list（防止漏入 `_ignore/` 等）
5. 任何 push / commit 失败的根因（如有）
6. **建议是否 tag**`v0.1-alpha.2-phase-b-done`（仅建议，不擅自 tag）

不能只写"已完成"——按 `ops-bridge` "回执必须含验证结果"。

---

PM-01 派单。OPS-01 等 PM "3 单落盘"信号 + DEV/QA 接单确认信号后立即执行。回执请落 `REPORT-20260509-017-OPS-to-PM.md`。

---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-007
sender: PM
recipient: DEV
priority: P0
thread_key: pre-s3-housekeeping
references:
  - REPORT-20260509-010-PM-to-ADMIN
layer: worker
---

# Sprint S3 启动前 housekeeping — A1+B1+C1 三件一起做

## ADMIN 5/9 11:29 决策（"按推荐做"）

= D1=A1 + D2=B1 + D3=C1 + D4=(a) 一起做 + D5=(yes) 先 commit ✅（commit 1 已完成 `6595427`）

## 三件交付物（全在本任务范围内）

### A1 — fcop 环境锁定

**文件**：`docs/integrations/fcop-standalone-zh.md`

**改动**：在合适位置追加一段「码流贡献者如何在 *自己机器上* 接 fcop-mcp」的指引片段。

**核心原则（不能违反）**：
- ⛔ 不要写"如何 *安装* fcop-mcp"具体步骤 → 那是 D:\FCoP 的事，硬规则 #5 红线
- ✅ 只写"指向 D:\FCoP README 安装 + 在你 *自己用户级* `~/.cursor/mcp.json` 挂上 + 验证已挂"
- ✅ 明确目标读者 = "本仓 contributors"（不是 codeflow 终端用户）

**草拟段落要点**：
1. 一句话说明：本仓不再 ship fcop 安装素材（链到硬规则 #5）
2. 推荐路径：上 [FCoP 上游仓](https://github.com/joinwell52-AI/FCoP) 按 README 走 PyPI 安装
3. 验证方式：在 Cursor 里看 MCP servers 列表，应能看到 fcop-mcp
4. 故障排查：如果你以前依赖本仓 codeflow-plugin/mcp.json，请删掉对它的引用

### B1 — 角色配置归集

**文件**：`docs/agents/CURRENT-ROLES.md`（新建）

**目的**：把当前 3 个分散的"角色信息来源"归集成一张表，让 contributors 一眼看清。**不动**任何源文件。

**结构要求**：
1. **Charter quote 块**：顶部引用 ADMIN 5/9 总纲（与 README 风格一致）
2. **现状声明**：当前活跃团队 = `dev-team`（PM/DEV/QA/OPS + ADMIN）；其他 3 个团队（media/mvp/qa-team）为模板素材，未启用
3. **三源对照表**（每个角色一行）：
   | 角色 | `.cursor/rules/` rule 文件 | `docs/agents/` 说明文档 | `codeflow-plugin/agents/` brief（v1 素材，等 §8.6 #5 迁移）|
4. **一致性声明**：哪个是 source of truth（强制 = `.cursor/rules/`，说明性 = `docs/agents/{ROLE}-01.md`，brief 素材 = `codeflow-plugin/agents/`）
5. **未来演进路径**：链向设计文档 §3.2 `roles.yaml` + §8.6 backlog #5（什么时候、怎么迁移到 `.codeflow/`）
6. **明确不做的事**：B1 阶段 *不* 创建 `roles.yaml`，*不* 创建 `.codeflow/`，等 FCoP Issue #2 闭环 + Sprint S3 启动

### C1 — tasks/ 历史归档

**目录**：`docs/agents/tasks/_archive/2026-04/`（新建）

**操作**：
1. 用 `git mv` 把以下 29 个文件移到 `_archive/2026-04/`：
   - 2026-04-03 ~ 04-08：2 个（`TASK-20260403-001-SYSTEM-to-PM.md` + `TASK-20260408-001-PM01-to-ADMIN01.md`）
   - 2026-04-20 ~ 04-25：27 个（包括所有 `TASK-20260420-*` / `TASK-20260421-*` / `TASK-20260422-*` / `TASK-20260423-*` / `TASK-20260425-*` / `REPORT-20260421-*` / `REPORT-20260425-001-FCoP-与-Bridgeflow-分仓-总结.md`）
2. **TASK-005 superseded 标记**：在 `TASK-20260509-005-PM-to-DEV.md` 顶部 frontmatter 后追加一个 `> ⚠️ SUPERSEDED by TASK-20260509-006` 注释块（不归档，留 root，但加旗）
3. 写 `docs/agents/tasks/_archive/README.md`，说明：
   - 归档原则：已闭环 + 距今 ≥ 7 天的 thread
   - 归档结构：按月 `_archive/YYYY-MM/`
   - 引用稳定性：归档后路径变化，但 git history 仍可追溯
   - 不归档的判断：active thread / 跨月仍在引用 / README.md

## 验收标准

| # | 项 | 验收方式 |
|---|---|---|
| 1 | A1 措辞不破坏硬规则 #5 | grep 检查无"安装步骤"关键词；只有"指向 D:\FCoP" |
| 2 | B1 文档完整 + 不创建 roles.yaml/.codeflow/ | `Test-Path docs/agents/CURRENT-ROLES.md`=True；`Test-Path .codeflow`=False |
| 3 | C1 - 29 个文件在 `_archive/2026-04/` | `(Get-ChildItem _archive/2026-04 -Filter '*.md').Count` ≥ 29 |
| 4 | C1 - tasks/ root 干净 | root 下只剩 README.md + 5/9 的 active 文件 + `_archive/`；总数 ~17 |
| 5 | C1 - TASK-005 加 SUPERSEDED 旗 | `Select-String SUPERSEDED docs\agents\tasks\TASK-20260509-005-PM-to-DEV.md` 有命中 |
| 6 | C1 - `_archive/README.md` 存在 | `Test-Path` |
| 7 | ReadLints 零错误 | 对所有改动文件 |
| 8 | 不动 `.cursor/rules/` 任何文件 | git diff 验证 |
| 9 | 不动 `codeflow-plugin/agents/` 任何文件 | git diff 验证 |
| 10 | 不删 `_ignore/audit_fcop_*` 任何文件 | git diff 验证 |

## 时间预算

≤ 60 分钟（A1: 10 + B1: 20 + C1: 30）

## DEV 回执要求

`REPORT-20260509-007-DEV-to-PM.md`，含：
- 三件改动清单
- 10 项验收逐项确认
- `git mv` 实际命令（便于 audit）
- 影响评估

DEV-01 立即执行。

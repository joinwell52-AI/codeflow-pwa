---
protocol: fcop
version: 1
sender: PM
recipient: DEV
thread_key: cleanup_codeflow_mcp_20260421
priority: P3
---

# 下架未被使用的 `codeflow_mcp.py`（pyautogui 面板 MCP）

## 背景

在 `anti_hang_triage_20260421`（AI 卡死分诊）线索中，顺带核查到
`codeflow-plugin/scripts/codeflow_mcp.py` 这个"用 pyautogui 模拟键盘打开 Simple Browser"
的独立 MCP，实际上：

- **零调用点**：除自身 + README 的"可选"段 + `cursor-shortcuts-scope.md` 的举例
  外，仓库内无任何代码 / 规则 / 任务文件调用它或其工具（`open_codeflow_panel` /
  `refresh_codeflow_panel`）。
- **作者自己也没装**：`C:\Users\Administrator\.cursor\mcp.json` 中只有 `codeflow`
  （主 MCP），**不含** `codeflow-panel`。
- **已有更干净的替代品**：`codeflow-desktop/cursor-extension/`
  下的 VSIX 扩展 `codeflow-panel-launcher`（命令 `codeflow.openPanel`）做的是
  **同一件事**，但走 VS Code 扩展 API，不依赖 pyautogui，不抢焦点、不卡死。
- **存在潜在卡死风险**：`pyautogui.getWindowsWithTitle` / `activate()` 无超时保护，
  与 ADMIN 关切的"AI 卡死"现象同源，留着它属于维持一个边际价值极低且有已知
  风险的死代码。

ADMIN 已批准清理。

## 验收条件

1. 仓库中不再存在 `codeflow_mcp.py` 及其专属依赖文件。
2. `codeflow-plugin/README.md` 不再推荐/描述这个 MCP。
3. 引用该文件作为"举例"的文档（`docs/cursor-shortcuts-scope.md` 及 `.en.md`）
   把举例替换为对应的 VSIX 扩展，避免读者去找一个已删除的路径。
4. `codeflow-plugin/requirements.txt`（主依赖）不受影响。
5. `codeflow-plugin/scripts/mcp_server.py` 不受影响。

## 具体动作

### 删除

- `codeflow-plugin/scripts/codeflow_mcp.py`
- `codeflow-plugin/requirements-codeflow-mcp.txt`

### 修改

- `codeflow-plugin/README.md`
  - 删除 "## 可选：轻量 MCP「只打开本机控制面板」" 整节（约第 93–121 行）
- `docs/cursor-shortcuts-scope.md` 第 29 行
  - 原文："**MCP**：例如仓库内 `codeflow-plugin/scripts/codeflow_mcp.py` 一类工具……"
  - 改为指向 VSIX 扩展 `codeflow-desktop/cursor-extension/`（`codeflow-panel-launcher`）
- `docs/cursor-shortcuts-scope.en.md` 第 29 行：同上（英文对译）

## 约束

- 不改 `codeflow-desktop/` 下的 VSIX 扩展，只做**引用替换**。
- 不动 `codeflow-plugin/scripts/mcp_server.py` / 主依赖 / 规则文件。
- 提交信息按项目惯例：`chore(plugin): remove unused pyautogui panel MCP (codeflow_mcp.py)`

## 交付物

- 完成清理后，回执 `TASK-20260421-002-DEV-to-PM.md`：
  - 列出实际删除与修改的文件路径
  - 说明是否需要重启服务（应为"否"——主 MCP 未受影响）
  - 自测结果（至少：主 MCP 仍能正常启动 / README 语法无破坏 / cursor-shortcuts-scope 两个语言版本对齐）

## 预期完成时间

一次会话内完成（清理范围小，低风险）。

# v0.2 Sprint 0 路线图：FCoP v1.0 charter alignment

> **状态**：PM 草拟（2026-05-09 23:14 ADMIN「按推荐」覆盖 C.1 + D.1 + B.1 后立即起草）
>
> **正式启动条件**：v0.1.0-rc.1 internal RC 完工（~3-5h 后）+ ADMIN 试用满意 + `fcop@1.0.0` final tag 在 PyPI 落地（5/16-5/20）
>
> **覆盖议题**：REPORT-20260509-023-PM-to-ADMIN §五 议题 B.1 / C.1 / D.1 实施层兑现

---

## §0 起源与触发

ADMIN 5/9 23:01 让 PM 看 [`FCoP issue #2 第二条 comment`](https://github.com/joinwell52-AI/FCoP/issues/2#issuecomment-4412811192) — 该 comment 是 ADMIN 自己作为 `@joinwell52-AI` FCoP maintainer 在 5/9 14:59 发的官方回复，宣告：
- `fcop@1.0.0-rc.1` 已 land（ branch on main）
- v1.0 final tag 在 5/16-5/20
- v1.0 ship **7 抽象**（agent / boundary / encoding / ipc-envelope / event / failure / review）—— 不是原来 ADR-0008 的 5 schemas
- 5 字段命运：layer ✅ ship 升级 Boundary / risk_level + Skill.tools deferred v1.1+ / needs_human + human_approval deferred v1.2+

ADMIN 5/9 23:14「按推荐」覆盖 PM 5 议题推荐 → 本 v0.2 sprint 0 路线图启动。

---

## §1 v0.2 sprint 0 一句话目标

**把 `packages/codeflow-protocol/` 从 v0.1 五 schemas（agent/task/review/session/skill）重构为 mirror upstream `fcop@1.0.0` 七 schemas（agent/boundary/encoding/ipc-envelope/event/failure/review）；同时把 `@codeflow/runtime` 的 NeedsHumanGate 改写为 Boundary capability check（候选）；保留 v0.1 internal RC 的所有 governance loop 行为不破坏。**

## §2 工作量预算

约 **5-7 工作日**（不含等 fcop@1.0.0 final 的等待时间）。

| 阶段 | 工作量 |
|---|---|
| Phase 0：等 fcop@1.0.0 final + 读 7 ADR + 7 schema | 1 天 |
| Phase 1：mirror 7 schemas 到 packages/codeflow-protocol/ + ajv compile | 1.5 天 |
| Phase 2：改写 runtime types + 删 5-schema 旧版 + 新增 boundary/encoding/ipc-envelope 4 schemas 实现 | 1.5 天 |
| Phase 3：NeedsHumanGate → Boundary capability check 改造（含 13 测试改写）| 1 天 |
| Phase 4：跨语言 fuzz 测试（TS↔Python schema 等价）| 0.5 天 |
| Phase 5：v0.2.0-alpha.1 release notes + README 更新 + issue #2 reply 更新 | 0.5 天 |

## §3 7 抽象 mirror 矩阵

| # | upstream 抽象 | upstream Schema | upstream ADR | CodeFlow 现状 | v0.2 sprint 0 动作 |
|---|---|---|---|---|---|
| 1 | **Agent**（lifecycle + identity + Boundary）| `agent.schema.json` + `boundary.schema.json` | ADR-0015 + 0020 | ✅ agent.schema.json 存在（layer 3 值，缺 Boundary）| 升级 agent.schema → mirror v1.0 + 新增 boundary.schema |
| 2 | **Encoding**（filename + frontmatter + workspace）| `encoding.schema.json` | ADR-0021 + 0022 | ❌ 隐式靠 fcop-mcp tool 约束 | 新增 encoding.schema 显式落档 |
| 3 | **IPC**（TASK/REPORT/ISSUE/REVIEW envelopes）| `ipc-envelope.schema.json` + `review.schema.json` | ADR-0017 | ⚠️ 现 task.schema.json 是单字段表 | 删 task.schema → 升 ipc-envelope.schema；review.schema 改 enum |
| 4 | **Event Model**（12 event types + subscribe）| `event.schema.json` | ADR-0018 | ❌ 在 RuntimeEvent 类型里，无顶级 schema | 新增 event.schema 落 12 event types |
| 5 | **Failure & Recovery**（4 failure × 5 recovery）| `failure.schema.json` | ADR-0019 | ❌ 错误类散在各模块 | 新增 failure.schema + 顶层归并错误类 |
| 6 | **Boundary**（capability bundle）| `boundary.schema.json` | ADR-0020 | ⚠️ 只有 layer 3 值 | 新增 boundary.schema + 10-token can/cannot bundle |
| 7 | **Audit**（REVIEW v1.0 minimal surface）| `review.schema.json` | ADR-0017 | ⚠️ enum 含 needs_human + 有 human_approval | 改 enum 为 v1.0 4 值 + 删 human_approval（→ v1.2+ 再恢复或永久退役）|

## §4 v0.1 资产命运

| v0.1 文件/类 | v0.2 命运 | 备注 |
|---|---|---|
| `packages/codeflow-protocol/schemas/agent.schema.json` | **保留 + 升级**（mirror v1.0 + 加 Boundary）| 主路径 |
| `packages/codeflow-protocol/schemas/task.schema.json` | **删** | 上升为 ipc-envelope.schema 子情形 |
| `packages/codeflow-protocol/schemas/review.schema.json` | **重写**（enum 改 4 值 + 删 human_approval）| upstream ADR-0017 mirror |
| `packages/codeflow-protocol/schemas/session.schema.json` | **删**（runtime-internal，不进 protocol 层）| runtime types 内保留 |
| `packages/codeflow-protocol/schemas/skill.schema.json` | **暂留**（v1.1+ 才进 protocol；v0.2 标 `pending-v1.1`）| 不强删，等 fcop@1.1 |
| `packages/codeflow-runtime/src/skill/SkillRegistry.ts` | **保留**（runtime-internal 实现，不依赖 protocol 层 skill schema）| OK |
| `packages/codeflow-runtime/src/skill/KernelDependencyValidator.ts` | **保留 + 改造**（用 Boundary check 替代 fcop 字符串匹配）| 工程内部细化 |
| `packages/codeflow-runtime/src/review/NeedsHumanGate.ts` | **改造**（→ BoundaryCapabilityCheck）或 **删**（如全部 use case 收敛到 Boundary）| 取决于 v0.2 实证 |
| `packages/codeflow-runtime/src/review/ReviewEngine.ts` | **改造**（4-enum + Boundary integration）| 主路径 |
| `packages/codeflow-runtime/src/review/ReviewWriter.ts` | **改造**（删 human_approval 写盘逻辑）| 同 |
| `packages/codeflow-runtime/src/review/__tests__/*` | 13 测试 → ~10 测试改写 + ~3 删 | E2E 保留 |

## §5 实施顺序（依赖图）

```
[Phase 0]                                      ← 等 fcop@1.0.0 final
   ↓
[Phase 1.a] mirror 7 schemas (filesystem)      ← 先把 spec 复制过来
[Phase 1.b] ajv compile + cross-language test   ← TS-Python equivalence
   ↓
[Phase 2.a] 删 task / session schema 文件        ← 旧文件下线
[Phase 2.b] 新增 boundary / encoding / ipc-envelope / event / failure types in runtime
[Phase 2.c] 升级 agent.schema → 含 Boundary
[Phase 2.d] 改写 review.schema → 4-enum + 删 human_approval
   ↓
[Phase 3.a] NeedsHumanGate code spike — 看哪些 use case 能收敛 Boundary
[Phase 3.b] 决策：删 / 改造 NeedsHumanGate
[Phase 3.c] ReviewEngine + ReviewWriter 适配 4-enum
[Phase 3.d] 13 测试改写（~10 改 + ~3 删）
   ↓
[Phase 4] 跨语言 fuzz 测试
   ↓
[Phase 5] v0.2.0-alpha.1 release notes + README 更新 + issue #2 reply 更新
```

## §6 风险点

| # | 风险 | 等级 | 兜底 |
|---|---|---|---|
| 1 | fcop@1.0.0 final 推迟到 5/20 之后 | 中 | v0.2 sprint 0 推迟启动；不阻塞 v0.1 internal RC 试用 |
| 2 | upstream 7 schemas 与 CodeFlow runtime 实际需求有 gap | 中 | 走 §3.3.1 deal flow（提 issue 上游）|
| 3 | NeedsHumanGate 用例无法全部收敛 Boundary | 高 | 保留 NeedsHumanGate 直至 v1.2，v0.2 标 `experimental-needs-human-fallback`，以 `?Boundary check first → ?fallback to needs_human` 双路径 |
| 4 | 13 测试改写时发现 review pipeline 副作用 | 低 | 13 测试都已 PASS，改写按 TDD 重做即可 |
| 5 | 跨语言 fuzz 测试发现 TS↔Python 不等价 | 中 | 提 issue #N 给 fcop maintainer，与 upstream 共修 |
| 6 | v0.2 工作量远超 5-7 天 | 中 | PM 提前请示 ADMIN 拆 v0.2 sprint 0+1 |

## §7 v0.2 启动判据（Phase 0 前置条件）

| # | 判据 | 满足条件 |
|---|---|---|
| 1 | v0.1.0-rc.1 internal RC 完工 | DEV 交 REPORT-028 + OPS 交 REPORT-029 + QA 交 REPORT-030 |
| 2 | ADMIN 试用 v0.1.0-rc.1 满意（核心三场景）| ① 双击启动 ② drop TASK 跑通 ③ Ctrl+C 优雅退出 |
| 3 | `fcop@1.0.0` final 在 PyPI 落地 | `pip install fcop==1.0.0` OK + spec/schemas/*.schema.json 可读取 |
| 4 | `fcop-mcp@1.0.0` final 在 PyPI 落地 | `pip install fcop-mcp==1.0.0` OK |
| 5 | 7 个 ADR (0015-0022) 状态全 Accepted | 上游 main 分支 ADR 文件状态 grep |
| 6 | issue #2 第三条 comment（CodeFlow team reply）已 post | upstream 看到 |

## §8 v0.2 完成判据

| # | 判据 | 测试方式 |
|---|---|---|
| 1 | `packages/codeflow-protocol/schemas/` 含 v1.0 全部 7 schemas | ls 验证 |
| 2 | TS↔Python schema 等价 fuzz 测试 PASS | npm test 跨语言用例 |
| 3 | `@codeflow/runtime` 全测试 PASS（含改写后 ~85 测试 ≈ 94 - 13 + 改写后 ~13）| npm test |
| 4 | `peerDependencies` pin `fcop>=1.0.0,<2.0.0` | package.json grep |
| 5 | NeedsHumanGate 命运决议（删 / 保留 fallback）写入 v0.2 release notes | 文档查 |
| 6 | issue #2 第四条 comment（CodeFlow v0.2 status update）已 post | upstream 看到 |
| 7 | v0.2.0-alpha.1 npm publish（首次公开发布）| `npm view @codeflow/runtime` |

## §9 §0.0 宪法对照（v0.2 不修改宪法本身）

| 宪法句 | v0.2 sprint 0 兑现 |
|---|---|
| 第 1 句「应用 fcop-mcp」| ✅ peerDependencies 锁 fcop@>=1.0,<2.0 + 7 schemas mirror |
| 第 2 句「轻量级 AI Runtime」| ✅ Boundary 比 NeedsHumanGate 轻量；删 human_approval 简化 review |
| 第 3 句「约束+能力+状态+权限 → 不会崩溃的协作宇宙」| ✅ Boundary 抽象正是这句的精确兑现 — v0.1 的 layer + needs_human 是粗近似 |
| 第 4 句「下达/审批/变更」| ✅ ADMIN governance triplet 不变；只是 review 走 4-enum 而非 5-enum |
| 第 5 句「按推荐」| ✅ v0.2 sprint 0 启动后 PM 仍按自约束工作，常规推荐自决 |

→ **v0.2 sprint 0 完工后，§0.0 宪法 5 句继续保持原话锁定不变，但「解读表」中第 1 句的「应用 fcop-mcp」一行可以加注「v0.2 起锁 fcop@>=1.0,<2.0；详见 v0.2-sprint0-roadmap.md」。这是解读层补充，不动 ADMIN 原话。**

## §10 PM 自约束触发

本路线图涉及：
- 公开 API breaking change（5→7 schemas）— **仍请示**项 → ADMIN 5/9 23:14「按推荐」已覆盖 C.1 = 等价于显式授权
- 跨 sprint 路线变更（v0.1→v0.2 路径）— **仍请示**项 → 同上覆盖
- 引入新外部依赖（fcop@1.0.0 + fcop-mcp@1.0.0）— **仍请示**项 → C.1 隐式覆盖（fcop 是已有依赖，只是版本切换；不算新依赖）
- 修改宪法本身 → ❌ 不修改原话；解读表附注属于 PM 自决（同前 4 句解读表更新先例）

✅ 全部覆盖在「按推荐」授权范围内，PM 自决起草本路线图（draft 状态，等 v0.1 完工后 ADMIN 正式启动开炮再 review）。

## §11 草稿状态声明

> **本文件状态 = `DRAFT-PM-PROPOSED`。** 直至 §7 全部 6 条启动判据满足 + ADMIN 显式开炮（写 `TASK-*-ADMIN-to-PM.md` 启动 v0.2 sprint 0），v0.2 sprint 0 不真实开干。

PM-01 草拟。
